<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 平时 AiClient 是在小爱进程里跑的,借的是宿主的权限;只有配置页的「测试连接」
         在本进程发请求 —— 少了这条它就一直连不上,而正常对话看起来完全正常。 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 无障碍服务被 MIUI 安全中心摘掉后自己写回来,见 AccessibilityGuard。
         签名级权限,装完要手动授一次(之后永久有效,重装才丢):
             adb shell pm grant io.mo.xiaoaiplug android.permission.WRITE_SECURE_SETTINGS
         不授也能跑 —— AccessibilityGuard 会降级到 su,只是每次多花几百毫秒。
         tools:ignore 是必须的:普通应用声明签名级权限,lint 默认当错误拦下来。 -->
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <queries>
        <package android:name="com.miui.voiceassist" />
    </queries>

    <application
        android:label="@string/app_name"
        android:allowBackup="false"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher">

        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/module_description" />
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.XiaoAiPlug"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|density|fontScale|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".config.ConfigProvider"
            android:authorities="io.mo.xiaoaiplug.config"
            android:exported="true" />

        <service
            android:name=".auto.UiAutoService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/ui_auto_service" />
        </service>
    </application>
</manifest>
